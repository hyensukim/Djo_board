package commons.DB;

public interface InsertQuery {
	/*
	 * Insert 쿼리 실행을 위한 기능 설계
	* return -> 변경된 레코드 수 ( int)
	* parameter -> mapper(mapper 구성요소의 name(String)으로 쿼리를 찾아 실행)용 매개변수 1
	* 					-> mapper에 담을 데이터를 담아 전송하는 DTO 객체용 매개변수 2
	*/
}
